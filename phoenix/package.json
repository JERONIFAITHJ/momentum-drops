{
  "name": "phoenix",
  "version": "1.0.0",
  "description": "Backend for flamingo",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run:ts-node": "ts-node src/index.ts",
    "dev": "nodemon --watch src --ext ts --exec \"yarn run run:ts-node\"",
    "start": "node dist/index.js",
    "build": "tsc"
  },
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.0.14",
    "nodemon": "^3.1.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
